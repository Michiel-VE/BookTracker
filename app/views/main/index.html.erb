<div class="flex flex-col lg:flex-row max-w-7xl mx-auto px-4 py-8 gap-6">
    <div class="w-full lg:w-4/5">
        <% if user_signed_in? %>
            <h1 class="text-3xl font-bold mb-6 text-green-asparagus">Welcome back, <%= current_user.full_name || current_user.email %>!</h1>
        <% end %>
    </div>


  <aside class="w-full lg:w-1/5">
    <%= render partial: "books/book_list", locals: { title: "liked books", books: @liked_books } %>
    <%= render partial: "books/book_list", locals: { title: "Read books", books: @read_books } %>
  </aside>


</div>
